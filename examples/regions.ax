struct Point
{
    x: i32
    y: i32
}

make(x: i32, y: i32) -> Point
{
    return Point { x: x  y: y }
}

copy_point(p: Point) -> Point
{
    return Point { x: p.x  y: p.y }
}

p = make(3, 4)
q = copy_point(p)
sum = q.x + q.y
