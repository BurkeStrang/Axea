struct Point
{
    i32 x
    i32 y
}

Point make(i32 x, i32 y)
{
    return Point { x: x  y: y }
}

Point copy_point(Point p)
{
    return Point { x: p.x  y: p.y }
}

p = make(3, 4)
q = copy_point(p)
sum = q.x + q.y

print("p = {p}")
print("q = {q}")
print("sum = {sum}")
