extern c puts(text: cstr)
extern c abs(x: i32) -> i32

greet(name: str) -> i32
{
    puts(name.to_cstr())
    return 1
}

called = greet("hello from axea")

message = String("owned strings work too")
called2 = puts(message.to_cstr())

magnitude = abs(0 - 42)
