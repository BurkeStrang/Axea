String build()
{
    name = String("Axea")
    name.append(" Language")
    return name
}

void appendOne(String name)
{
    name.append("!")
}

i32 useStr(str s)
{
    return 1
}

name = build()
lengthAfterBuild = name.length
called = appendOne(name)
lengthAfterAppend = name.length
viaStr = useStr(name)
other = String(name)
otherLength = other.length

print("name = {name}")
print("lengthAfterBuild = {lengthAfterBuild}")
print("lengthAfterAppend = {lengthAfterAppend}")
print("viaStr = {viaStr}")
print("other = {other}")
print("otherLength = {otherLength}")
